<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.xiaozhi.message.dal.mysql.mapper.ConversationMapper">

    <!-- 当前用户的会话按最后对话时间倒序，带角色名 -->
    <select id="selectPage" resultType="com.xiaozhi.common.model.bo.ConversationBO">
        SELECT
            c.sessionId,
            c.userId,
            c.roleId,
            r.roleName,
            c.title,
            c.createTime,
            c.updateTime
        FROM sys_conversation c
        LEFT JOIN sys_role r ON c.roleId = r.roleId
        WHERE c.userId = #{userId}
        <if test="roleId != null">
            AND c.roleId = #{roleId}
        </if>
        ORDER BY c.updateTime DESC
    </select>
</mapper>
